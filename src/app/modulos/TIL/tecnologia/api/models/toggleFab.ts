export interface toggleFab {
    icon: string;
    tool: string;
    state?: string;
    enabled?: boolean; 
    color?: string;
    functionName: string
    matBadge?: string;
    matBadgeColor?: string;
}